apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-pvc-h
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
  volumeName: static-pv-h
